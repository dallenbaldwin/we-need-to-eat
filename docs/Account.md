look into using [https://github.com/soldair/node-qrcode](https://github.com/soldair/node-qrcode) to generate a QR code that others can use to add them as a friend

the QR code would just be a link to the app's friend request page with the user's friend code as a query param, which could then be used to find the user